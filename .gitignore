# Node
node_modules/
dist/
*.log
.env
.env.local

# Xcode
DerivedData/
build/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/
.swiftpm/
Package.resolved

# OS
.DS_Store

# Local runtime data
server/data/
*.sqlite
*.sqlite-journal

# XcodeGen output is regenerated by `xcodegen generate` — but committed so the
# project opens without extra tooling.
# (TripBudget.xcodeproj IS tracked on purpose.)
ios/build/
ios/build-device/
ios/build-device2/
ios/DerivedData/
